name: receipt_scanner
description: Приложение для сканирования чеков

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.0
  google_mlkit_text_recognition: ^0.7.0
  path_provider: ^2.0.11
  image: ^4.0.14
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
